var Options = {
    ambientLevel: 0.08, //ambient noise level
    noiseLevel: 0.08, //noise level
    noiseEnabled: true, //noise enabled
    renderDistance: 8  //distance to render tiles
};

module.exports = Options;
